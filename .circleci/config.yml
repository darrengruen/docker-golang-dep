version: 2.1
jobs:
  build:
    docker: 
      - image: ubuntu:18.04
    steps:
      - run: echo ${DOCKER_PASS} | docker login -u ${DOCKER_USER} --password-stdin
      - run: make build
      - run: make test
      - run: make push